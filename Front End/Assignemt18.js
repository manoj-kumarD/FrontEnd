let num="100";
if(isNaN(num)){
    console.log(`The ${num} is number`);
    document.write(`The ${num} is number`);
}
else {
    console.log(`The ${num} is ${typeof(num)}`);
    document.write(`The ${num} is ${typeof(num)}`); 
}